
{{#partial "content"}}


        <h1>Add a Member!</h1>

        <form action="/members/new" method="post">
            <p>
                <label>Team:</label>
                <select name="meaning">
                    {{#each teams }}
                        <option value="{{meaning}}" {{#selected}}selected="selected"{{/selected}}>{{ meaning }}</option>
                    {{/each}}
                </select>
            </p>
            <label for="inputMember">Enter Member Name</label>
            <input id="inputMember" name="inputMember" type="text">
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
{{/partial}}



{{> layout.hbs}}